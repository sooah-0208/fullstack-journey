jQuery
```
$(function(){
  //이 중괄호 안쪽이 jQuery 영역

});
```
---

jQuery 영역에 입력한 값은 console에 안 불러와짐
jQuery 영역 like 지역변수
그럼 어떻게 콘솔에 불러와요?

```
console.log($('section'));                  // jQuery 객체
console.log($('section')[0]);               // DOM 요소 --> 화면에서 구조까지 보여줌
console.log(document.querySelector('section')); // DOM 요소
```

callback함수(익명함수) `(){}`는 못 씀
jQuery 안에는
`function name(){}` / `()=>{}(에로우함수)` 두가지 사용 가능함


`$('section').html(html);`
-> $(대상): 'section'이라는 놈에게 
-> .(함수): html 형식으로
->(요소): 요소를 넣어줘

이벤트는 중복이 가능함
=> 중복을 없애기 위해
on off 가 필요함
`$(태그/클래스/#아이디).off()` -> 모든 이벤트를 없앤다
`$(태그/클래스/#아이디).off().on("실행된 이벤트")` -> ""안의 이벤트를 없앤다
만약
`$('button').click(function(e){어쩌구저쩌구} `가 여러개라서 클릭 이벤트가 여러개일 때 사용하는것
