dev - develop 개발
ops - operates 운영

CI/CD
예시
- Hudson & Jenkins(build하는 프로그램)
- Git Actions(느림)

dockerfile => jenkins -> docker 홈페이지 찾으면 있음

[Jenkins 시작하기](https://github.com/sooah-0208/fullstack-journey/tree/main/practice_file/0219)

container 생성
`docker compose up -d`
jenkins비번 확인
`docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword `

docker volumes -> jenkins_home -> jobs에서 만든 build확인 가능 && workspace -> 만든 이름보면 내가 git에 올린 애들 확인 가능