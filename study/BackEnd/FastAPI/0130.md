![alt text](image-4.png)

*라우터*
외부 요청을 분배처리하는게 라우터

Packet Switching
네트워크에서 물리적으로 res/req 전달하는 과정
이 packet을 열면 내부 내용을 훔쳐볼 수 있음 -> 해킹이 여길 터는 거
HTTP에서 이걸 막아주는게 SSL(Secure), ssl이 추가된 프로토콜이 HTTPS임

port = TCP

![alt text](image-5.png)