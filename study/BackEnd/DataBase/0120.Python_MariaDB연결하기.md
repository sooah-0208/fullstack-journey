폴더 하나 생성
기본 파이썬 초기화
- uv init .
MariaDB다운로드
- uv add mariadb
`pyproject.toml`에서 mariadb 설치되었는지, version확인하면 됨

# Module Imports
import mariadb
import sys

```
# Connect to MariaDB Platform
try:
    conn = mariadb.connect(
        user="db_user",
        password="db_user_passwd",
        host="192.0.2.1",
        port=3306,
        database="employees"

    )
except mariadb.Error as e:
    print(f"Error connecting to MariaDB Platform: {e}")
    sys.exit(1)

# Get Cursor
cur = conn.curso
```

conn의 내용은 본인 SQL 정보 쓰면 됨


try란?
에러(예외)를 잡기 위한 문법

try:
    # 에러가 날 수도 있는 코드
except:
    # 에러가 나면 여기 실행

🔥 만약 try가 없으면?  
👉 프로그램 바로 종료 (크래시)  
🔹 try가 꼭 필요한 이유
DB 연결  
파일 열기  
네트워크 요청  
사용자 입력 처리  
👉 실패 가능성이 있는 작업은 무조건 try