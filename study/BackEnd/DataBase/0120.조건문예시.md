-- 0. 사번이 10001인 직원의 사번과 이름 조회
SELECT emp_no, first_name FROM edu.employees
WHERE emp_no = 10001;

-- 1. d005 부서 매니저의 사원번호, 부서번호 추출하시오.
select emp_no, dept_no from dept_manager
WHERE dept_no = 'd005';

-- 2. d003 부서 소속(담당)이 아닌 매니저들의 사원번호, 부서번호 추출하시오.
select emp_no, dept_no from dept_manager
WHERE dept_no != 'd003';

-- 3. 연봉이 150,000 이상인 사원들의 사원번호, 연봉 추출하시오.
select emp_no, salary from salaries
WHERE salary >= 150000;

-- 4. 1986년 이후에 입사한 사원의 사원번호, 입사일, 이름 추출하시오.
select emp_no, hire_date, first_name from employees
WHERE hire_date >= '1986-01-01';

#다른 방법
select emp_no, hire_date, first_name from employees
WHERE SUBSTR(hire_date, 1, 4) >= 1986
ORDER BY 2;

-- 5. 1990년 이후부터 매니저로 근무하고 있는 사원들의 사원번호, 부서번호, 매니저 시작날짜 추출하시오.
select emp_no, dept_no, from_date from dept_manager
WHERE from_date >= '1990-01-01';

#다른 방법
WHERE from_date LIKE '199%';


-- 6. 1990년 이전 입사한 사원들의 사원번호, 입사일 추출하시오.
select emp_no, hire_date from employees
WHERE hire_date <= '1990-12-31';

#여기도 SUBSTR 가능

-- 7. 1990년 이후 입사한 남자 사원의 사원번호, 성별, 입사일 추출
select emp_no, gender, hire_date from employees
WHERE gender ='M'
AND hire_date >= '1990-01-01';

-- 8. 1990년 이후부터 연봉을 60,000 이상 받는 사원의 사원번호, 연봉, 연봉 시작일 추출
select emp_no, salary, from_date from salaries
WHERE from_date >='1990-01-01'
AND salary>=60000;

-- 9. 001 부서와 d002 부서 매니저의 사원번호, 부서번호 추출
select emp_no, dept_no 
from dept_manager
where dept_no in ('d001','d002');

-- 10. 직책이 staff이거나 engineer인 사원의 사원번호, 직책 추출
select emp_no, title from titles
WHERE title In ('staff', 'engineer');

-- 11. d003 부서 소속(담당)이 아닌 매니저의 사원번호, 부서번호 추출
select emp_no, dept_no from dept_manager
WHERE dept_no NOT IN ('d003');

#또는
WHERE dept_no <> 'd003';

-- 12. 연봉이 60,000 이상 70,000 이하인 사원의 사번, 연봉 추출
select emp_no, salary from salaries
WHERE salary>=60000 and salary<=70000 ;

#또는
WHERE salary BETWEEN 60000 AND 70000 ;

-- 13. d001 부서와 d002 부서 매니저의 사번, 부서번호 추출
select emp_no, dept_no from dept_manager
WHERE dept_no IN('d001','d002');

-- 14. d001 부서와 d002 부서가 아닌 매니저의 사번, 부서번호 추출
select emp_no, dept_no from dept_manager
WHERE dept_no not IN('d001','d002');

-- 15.
select emp_no, first_name from employees
WHERE first_name LIKE 'B%';

-- 16.
select emp_no, first_name from employees
WHERE first_name LIKE '_r%';

-- 17.
select emp_no, first_name from employees
WHERE first_name LIKE '%i';

-- 18.
select emp_no, first_name from employees
WHERE first_name not LIKE 'B%';