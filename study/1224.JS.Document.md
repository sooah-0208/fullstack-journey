# javascript로 웹 화면 조작하기

1.js파일 생성하여 html `<head>`에 `<script>`넣기
어? 안 나오는데요? 
코드는 위에서부터 순서대로 읽어지기 때문에 스크립트가 태그 위에 있기때문에 태그가 없는 상태로 실행해서 오류가 남
그럼 어떡하죠?
body tag 안에 onload 넣기.
onload란? tag를 다 실행 한 후에 onload="얘" 얘를 실행하겠습니다. 하는 함수


2. 콘솔에서 태그 위치 찾는 법
`document.getElementsByTagName('section')[0];`
-> []가 붙는 이유?
=> body안에 같은 태그가 여러개 있을 수 있기 때문에 기본적으로 `배열`자료형을 가짐

3. document에 요소 넣는 방법
document.getElementsByTagName("section")[0].innerHTML = 요소명


예시)

```
**필수**
function 하고싶은 이름(){
let div ='';

1)
// for(i=0; i<25; i++){
//     if(i%2 === 0){
//         div+=`<div class="bg1"></div>`
//     }else{
//         div+=`<div class="bg2"></div>`
//     }
// }

2)
 for(i=0; i<25; i++){
    let css ='1';
    if(i === 0) css='2'
    div+=`<div class="bg${css}"></div>`
 }

3)
for(i=0; i<25; i++){
    div+=`<div class="bg${i%2 === 0 ? '2' : '1'}"></div>`
}
**필수**
document.getElementsByTagName("section")[0].innerHTML =div
}
```
*지금은 1)이 먼저 생각나니까 코드를 다 일단 짜보고 어떻게 줄일 수 있을까? 생각해보기*
->가독성 좋게 만들기 위함
